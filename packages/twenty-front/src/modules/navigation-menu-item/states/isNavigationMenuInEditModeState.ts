// Dead-symbol stub: Fuse code referenced this atom, but it was never committed.
// Stubbed as a readonly false atom so consumers can import without runtime error.
// TODO(wave-3-cleanup): remove or implement the feature.
import { atom } from 'jotai';
export const isNavigationMenuInEditModeState = atom(false);
