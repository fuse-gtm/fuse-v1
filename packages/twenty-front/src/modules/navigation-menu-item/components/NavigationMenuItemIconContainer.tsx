// Dead-symbol stub: feature was planned but never implemented.
// Renders null; build passes, runtime no-op.
// TODO(wave-3-cleanup): remove consumer or implement.
import type { ReactNode } from 'react';
export const NavigationMenuItemIconContainer = (_props: { [key: string]: unknown; children?: ReactNode } = {}): JSX.Element | null => null;

import { styled as __fuseStyledIconContainer } from '@linaria/react';
export const StyledNavigationMenuItemIconContainer = __fuseStyledIconContainer.div``;
