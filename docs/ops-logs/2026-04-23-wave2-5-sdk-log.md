# Wave 2.5 SDK Sub-Wave — Cherry-pick Log

Branch: `integration/upstream-wave2-5-sdk`
Base: `02ca1296f5` (post-wave-2-closeout main)
Founder direction (2026-04-23): ship SDK refactor now; solo-founder cadence doesn't justify deferring. Decisions codified in `bfcba648a3` (Q1-Q4 context commit on this branch).

## Summary
- Attempted: 3
- Successful: 3
- Skipped: 0

## Commits landed (in dependency order)

| Original SHA | Cherry-picked SHA | Subject |
|---|---|---|
| `4ea2e32366` | `7775e030e9` | Refactor twenty client sdk provisioning for logic function and front-component (#18544) |
| `c26c0b9d71` | `1a0f6f1426` | Use app's own OAuth credentials for CoreApiClient generation (#19563) |
| `baf2fc4cc9` | `78c0b5ac88` | Fix sdk-e2e-test: ensure DB is ready before server starts (#19583) |

## Strategy note

Used `--strategy-option=theirs` on the recursive merge strategy to aggressively favor upstream on file moves and content divergence. Protected-path conflicts (partner-os, enterprise, FeatureFlagKey, 7 branded files) were set to fall back to HEAD — but **none of the three commits actually touched any protected path** (verified via pre-continue `git diff --cached --name-only` filter). Manual resolution handled three categories:

1. **modify/delete (theirs kept file)** — 5 files where Fuse had deleted content that upstream modified. Accepted upstream's version via `git checkout --theirs` + `git add`.
2. **both added (AA)** — 1 file (`logic-functions/create-hello-world-company.ts`). Took upstream.
3. **UD (upstream deleted, ours modified)** — 1 file (`resolve-app-access-token.ts`). Accepted delete via `git rm`.

This is a one-shot strategy for this sub-wave; future waves revert to the default favor-upstream-on-logic-only pattern.

## Skipped

_None._

## Invariants (post-cherry-pick)

| Check | Result |
|---|---|
| Partner-os diff | PASS (0 lines) |
| Branded-file MD5 | PASS (all 3 unchanged) |
| Enterprise stubs | PASS (count=2 preserved) |
| FeatureFlagKey.ts | PASS (0 lines diff) |
| `IS_PARTNER_OS_ENABLED` | PASS |
| create-twenty-app CLI bin | PASS (`"bin": "dist/cli.cjs"`) |

## Schema deltas

GraphQL/DTO/resolver/generated-metadata file touches: 2

- `packages/twenty-client-sdk/src/metadata/generated/schema.graphql`
- `packages/twenty-server/src/engine/core-modules/application/application-development/application-development.resolver.ts`

Both are regeneration / SDK-related; no breaking partner-os API surface change.

## Directory deltas

- `packages/twenty-apps/` added new subdir `hello-world/` (4 files) alongside existing `examples/hello-world/` (29 files). This is a **known residual** from UD-style conflicts: upstream kept modifying the pre-refactor `hello-world/` path even as the primary commit moved it to `examples/hello-world/`. Because `theirs` resolution recreated the old path at each modify/delete site, both directories now coexist. This should be reviewed and likely consolidated in a follow-up — upstream's own refactor didn't fully complete the move before the later commits we took. Flagged below.
- SDK file count: 1332 → 1335 (+3 net)
- Total files changed from origin/main: 92

## Verification status

Deferred to CI (sandbox network-restricted, can't `yarn install`). See PR description for yarn.lock drift P0 note.

## Conflict resolutions worth human review

1. **`packages/twenty-apps/hello-world/` vs `packages/twenty-apps/examples/hello-world/`** — old path survives with 4 files (agents, page-layouts, front-components, CI workflow). Upstream's refactor intended the move but follow-up commits still referenced the old path. Review whether to delete the old path entirely or whether `twenty-sdk` tooling still expects it.
2. **`packages/twenty-sdk/src/cli/utilities/auth/resolve-app-access-token.ts`** — deleted per commit 3 (`baf2fc4cc9`). Fuse had local mods; accepted delete per "favor upstream on file moves". New entry point is `exchange-credentials-for-tokens.ts` + `auth/index.ts` barrel.
3. **`packages/create-twenty-app/src/utils/test-template.ts{,.spec.ts}`** — re-added from upstream. HEAD had deleted these (likely from a prior wave); upstream's follow-up modified them. Accepted upstream version. Confirm whether test-template flow is wanted in Fuse.

## Post-execution orchestrator cleanup

Commit `d138cdd3a7` — removed the duplicate `packages/twenty-apps/hello-world/` directory (4 files: agents, page-layouts, front-components, CI workflow) that had lingered from the `--strategy-option=theirs` UD resolution. The canonical `packages/twenty-apps/examples/hello-world/` (29 files) is the sole hello-world example on this branch. Verified zero `.ts/.tsx/.js/.json` references to the old `twenty-apps/hello-world/` path remain in `packages/` — only docs mention the legacy path, which is expected historical context.

## Guardrail A — telemetry / egress scan (PASS)

Ran the 3-grep diff scan against `origin/main...HEAD`:

1. Pattern `twenty\.com|ENTERPRISE_VALIDITY_TOKEN|twentyhq|clickhouse|posthog|segment|mixpanel|amplitude|\.track\(|analytics\.track|identify|page` — matches found, all classified OK:
   - 6 matches in `docs/fuse-upstream-patch-ledger.md`, `docs/ops-logs/2026-04-23-wave2-deploy-readiness.md`, `packages/twenty-docs/developers/extend/capabilities/apps.mdx`, `packages/create-twenty-app/README.md` — pure documentation links pointing to `docs.twenty.com` / `github.com/twentyhq/twenty`.
   - 2 matches in `packages/create-twenty-app/src/utils/test-template.ts`:
     - `TWENTY_API_URL = process.env.TWENTY_API_URL ?? 'http://localhost:2020'` — configurable, defaults to local. No hard-coded twenty.com endpoint.
     - Generated GitHub Actions workflow references `twentyhq/twenty/.github/actions/spawn-twenty-docker-image@main`. This is **scaffolded into user-generated apps**, not executed by Fuse runtime. Users who scaffold apps with `create-twenty-app` get a CI workflow that pulls the spawn action from the upstream repo. Not a runtime egress concern; worth tracking as a downstream brand-surface item (nit — future work could fork the Action into `fuse-gtm/fuse-v1`).
2. Pattern `phone-home|verifyEnterprise|validateLicense|licenseValidity` — **CLEAN** (no matches).
3. `package.json` / `yarn.lock` new deps matching `posthog|segment|@clickhouse|mixpanel|amplitude` — **CLEAN** (no new telemetry deps).

Verdict: PASS.

## Guardrail B — partner-os surface (PASS)

`git diff origin/main...HEAD -- packages/twenty-server/src/modules/partner-os/` returns 0 lines. Partner-os is untouched.

Verdict: PASS.

## SDK-specific integrity checks (PASS)

| Check | Result |
|---|---|
| `create-twenty-app` CLI bin | PASS — `"bin": "dist/cli.cjs"`, `"main": "dist/cli.cjs"` |
| `twenty-sdk` exports | PASS — 6 named subpath exports (`./define`, `./front-component`, `./ui`, `./cli`, `./front-component-renderer`, `./build`), each with types + ESM + CJS. Bin exposed as `{ "twenty": "dist/cli.cjs" }` |
| `twenty-client-sdk` exports | PASS — 3 named subpath exports (`./core`, `./metadata`, `./generate`), matching `typesVersions` |
| `examples/hello-world` package.json | PASS — valid, declares `dependencies` on `twenty-client-sdk@0.9.0` + `twenty-sdk@0.9.0` with `devDependencies` portal overrides pointing at the workspace packages. (Published version tags don't match current canary versions — `twenty-sdk 1.23.0-canary.2`, `twenty-client-sdk 0.7.0-canary.0` — but portal paths take precedence in local workspace, matching upstream pattern.) |
| `examples/hello-world` src layout | PASS — 12 top-level subdirs (`__tests__`, `agents`, `fields`, `front-components`, `logic-functions`, `navigation-menu-items`, `objects`, `page-layouts`, `roles`, `skills`, `views`) + `application-config.ts` |
| Stray `twenty-apps/hello-world/` | CLEAN — orchestrator commit `d138cdd3a7` removed the duplicate |
| `resolve-app-access-token.ts` replacement | PASS — file removed; `auth/index.ts` barrel now exports `ensureAppAccessTokenIsValidOrRefresh`, `ensureAppRegistration`, `exchangeCredentialsForTokens`. New `exchange-credentials-for-tokens.ts` uses `configService.getConfig().apiUrl` (configurable, no hard-coded endpoint). No stale imports anywhere in `packages/`. |
| `test-template.ts` reintroduction | ACCEPT — scaffolds vitest config + test seed into generated apps. Only references configurable `TWENTY_API_URL` and `TWENTY_API_KEY` env vars. Contains one hard-coded seed JWT (`SEED_API_KEY`) used only for local integration test bootstrap; it's a workspace dev-seeder JWT signed against a known test secret, not a production credential. Emitted GitHub Actions workflow references `twentyhq/twenty` Action (see Guardrail A nit). |

## Package.json JSON validity (PASS)

All modified package.jsons parse cleanly:
- `packages/create-twenty-app/package.json`
- `packages/twenty-sdk/package.json`
- `packages/twenty-client-sdk/package.json`
- `packages/twenty-apps/examples/hello-world/package.json`
- root `package.json`

## Deep file review (PASS)

Files changed vs `origin/main`: **89**. Subsystem breakdown:

| Subsystem | Count |
|---|---|
| `packages/twenty-sdk/` | 31 |
| `packages/twenty-server/` | 26 |
| `packages/twenty-client-sdk/` | 7 |
| `packages/twenty-apps/` | 7 |
| `packages/create-twenty-app/` | 6 |
| `docs/ops-logs/` | 2 |
| `.github/workflows/` | 2 |
| `packages/twenty-front-component-renderer/` | 1 |
| `packages/twenty-front/` | 1 |
| `packages/twenty-docs/` | 1 |
| `.github/actions/` | 1 |
| root (`package.json`, `yarn.lock`) | 2 |
| `docs/` (ledger, spec) | 2 |

Server-side changes (26) concentrate in `sdk-client/`, `logic-function-drivers/`, `application-install/`, `application-sync`, and `upgrade-version-command/1-20/` (new migration scaffolding for SDK client regeneration). Auth + core-engine modules received minor refactor edits (calendar-channel data-access module swap, removal of `BillingGraphqlApiExceptionFilter` import) — no Fuse enterprise-stub surface affected.

No stale imports of removed `resolve-app-access-token` symbol. No runtime references to the old `twenty-apps/hello-world/` path. No new telemetry dependencies added.

## Concerns / watch items

1. **Scaffolded CI workflow uses `twentyhq/twenty` Action** (`packages/create-twenty-app/src/utils/test-template.ts:251`) — downstream brand-surface nit. Users scaffolding apps via Fuse's CLI will emit a workflow that pulls from the upstream repo. Track as future follow-up; not a blocker.
2. **`examples/hello-world/package.json` declares `twenty-sdk@0.9.0` / `twenty-client-sdk@0.9.0`** — these published-version tags don't match current canary versions. Portal `devDependencies` keep local dev working; but `yarn install` in a user-scaffolded copy of this example (outside the workspace) would resolve from npm. Upstream ships this pattern, so matches their behaviour. Worth tracking if Fuse ever publishes its own SDK to a registry.
3. **`SEED_API_KEY` JWT literal in `test-template.ts`** — scaffolded into user test configs. It's a long-lived dev JWT (`exp: 2066`) signed against a known local seed secret; only usable against a dev seeder–loaded instance. Upstream-authored, acceptable risk.

Verdict: PASS with three tracked nits (none gating merge).

## Overall verdict

All six checks pass. Ready for merge into main pending any reviewer feedback on nits above.
